# Fail2Ban filter for PHP/admin panel probes
# No PHP on this server - any .php request or admin panel probe is malicious

[Definition]

failregex = ^<HOST> \- \S+ \[\] \"(GET|POST|HEAD) \S*(\.php|phpmyadmin|phpinfo|adminer|/manager/|/solr/|/admin/login|/administrator/|ckeditor|elfinder|tinymce|fckeditor)\S* \S+\" \d+ \d+ \"[^\"]*\" \".*\"$

ignoreregex =
